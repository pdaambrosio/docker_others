# Dockerfile Examples

Dockerfile examples for building images with Docker + Kubernetes on CentOS 7.

## Files

- `dockerfile_docker` — Installs Docker CE, Kubernetes tools (kubelet, kubeadm, kubectl) on CentOS 7
- `dockerfile_docker2` — Variant of the above setup
